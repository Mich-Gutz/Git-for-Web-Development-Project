Michelle-Gutierrez.txt
<!-- Questions to Submit in today's file first-lastname.txt --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? Tags with meaning to better describe our code, they give meaning to our web page.
    2. What is HTML used for? It's the markup language we use for web development, it displays data in graphical form.
    3. What is an attribute and where do we put it? Special flags to include information that we don't want to show on the page. Used next to the opening tag.
    4. What is the h1 tag used for? How many times should I use it on a page?to add the headings, it's a way to present the subjects matter. Shouls only be used once.
    5. Name two tags that have required attributes <a> <img>
    6. What do we put in the head of our HTML document? The head tag to start <h1>, after the declaration <!DOCTYPE html>
    7. What is an id? An attribute that specifies a unique id in the element.
    8. What elements can I add an id to? all elements
    9. How many times can I use the same id on a page? Not more than once.
    10. What is a class? Attribute used to specify 1 or more class names for an element.
    11. What elements can I add a class to? It may be used on any element.
    12. How many times can I use the same class on a page? As many as you would like.
    13. How do I get my link to open in a new tab? Set the target attribute to _blank EG: <a href:"https://www.google.com/"target_"blank">
    14. What is the alt attribute in the image tag used for? To set a message in case the image does not show on the page.
    15. How do I reference an id? Using a # symbol followed by the name of an id.
    16. What is the difference between a section and a div. Section tab reffers to sections of a document as opposed to div which may reffer to a division or a section grouping block elements.
    17. What is CSS used for? It's used for adding style and a layout to the page.
    18. How to we select an element? Example - every h2 on the page. We use them according to the semantic html layout.
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. id is used to identify 1 element and class identifies more than 1. Ex: class used to style the text of various elements with the same class. ids are unique, an example is a footer or header and images.
    20. How do we select classes in CSS? you write period and the name of the class, add it in the class attribute in your html, choose according to the actions you want on the specific tag
    21. How do we select a p element with a single class of “human””? p class= "human"
    22. What is a parent child selector? When would this be useful? the selector that selects all elements that are a direct child of the specified element. Wehan making a list embeded in another list.
    23. How do you select all links within a div with the class of sidebar? <div class="navigation-div">
    24. What is a pseudo selector? a selector to define a special state of an element. EG: style when mousing over, visited and unvisited links, when focusin on an element.
    25. What do we use the change the spacing between lines? the line-height property   line-height: 3px
    26. What do we use to change the spacing between letters? letter-spacing property letter-spacing:3px
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform property  text-transform:capitalize, text-transform:lowercase, text-transform:capitalize
    28. How do I add a 1px border around my div that is dotted and black? name the div, and put in brackets block border:black dotted
    29. How do I select everything on the page? using a * selector
    30. How do I write a comment in CSS? /*your comment*/
    31. How do I find out what file I am in, when I am using the command line? pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rf, you don't get a notification about being sure to delete.
    34. Why should I use version control? It helps you  manage changes to a source code over time tracking every change made by contributors.
    35. How often should I commit to github? around a couple of times every hour or when you add a feature worth commiting.
    36. What is the command we would use to push our repo up to github?  git commit -m
    37. Walk me through Lambda's git flow. 

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 



   --> 